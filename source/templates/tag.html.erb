---
pageable: true
per_page: 20
---

<%= partial 'partials/list_articles', locals: {
    need_articles: page_articles,
    need_title: "Browsing &ldquo;#{tagname}&rdquo; tag",
} %>

<%= partial 'partials/pagination', locals: {
    need_articles: page_articles,
    placeholder: "tagname",
    url_schema: "/posts/tag/{placeholder}/",
} %>
