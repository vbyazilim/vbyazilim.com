---
pageable: true
per_page: 20
---

<%= partial 'partials/list_articles', locals: {
    need_articles: page_articles,
    need_title: "XXX",
} %>

<%= partial 'partials/pagination', locals: {
    need_articles: page_articles,
    placeholder: "year",
    url_schema: "/posts/year/{placeholder}/",
} %>
